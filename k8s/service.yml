apiVersion: v1
kind: Service
metadata:
  name: tecverify-fe
spec:
  type: NodePort
  selector:
    app: tecverify-fe
  ports:
    - port: 443
      
      protocol: TCP
      
      name: https
      
      
---
apiVersion: v1
kind: Service
metadata: 
  name: tecverify-be
spec:
  type: ClusterIP
  selector:
    app: tecverify-be
  ports:
    - port: 5000  
      targetPort: 5000